<div class="empty-state">
  <h1>{{ '404.title' | t }}</h1>
  <p>{{ '404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn btn--navy">{{ '404.link' | t }}</a>
</div>

{% schema %}
{
  "name": "404 page",
  "settings": []
}
{% endschema %}
